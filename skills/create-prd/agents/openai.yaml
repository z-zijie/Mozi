interface:
  display_name: "Create Operator PRD"
  short_description: "Generate or revise a normalized operator PRD"
  default_prompt: "Use $mozi:create-prd to generate a normalized PRD from a brief prompt, or revise an existing PRD from review feedback."
