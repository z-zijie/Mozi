# Mozi OpenCode Notes

Mozi's OpenCode package lives in `.opencode/plugins/mozi.js`. It registers the repository `skills/` directory and injects `skills/using-mozi/SKILL.md` as startup context.

Install instructions live in [.opencode/INSTALL.md](../.opencode/INSTALL.md).
