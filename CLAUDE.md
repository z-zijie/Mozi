# Mozi - Claude Code Context

Follow [AGENTS.md](AGENTS.md). This repository is a root-level multi-harness plugin modeled after `superpowers`.

Load Mozi workflows from `skills/`. Use the relevant skill for `$mozi:create-prd`, `$mozi:review-prd`, `$mozi:create-spec`, and `$mozi:review-spec` requests.
