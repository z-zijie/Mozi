interface:
  display_name: "Create Operator PRD"
  short_description: "Generate a normalized operator PRD from a brief prompt"
  default_prompt: "Use $mozi:create-prd to generate a normalized PRD for an operator from a brief prompt."
