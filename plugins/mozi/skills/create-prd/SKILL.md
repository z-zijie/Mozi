---
name: create-prd
description: Create the Mozi AddRelu PRD stub. Use when the user invokes `$mozi:create-prd` or asks to create the PRD for the AddRelu operator workflow; the initialized output is `docs/mozi/addrelu/prd.md` as an empty file.
---

# Create PRD

When invoked, ensure `docs/mozi/addrelu/prd.md` exists in the repository.

For the current initialized workflow, leave the PRD empty. Do not expand the AddRelu requirements into PRD content unless the user explicitly asks for a populated PRD.

If the file already exists and has content, preserve it unless the user explicitly asks to reinitialize or overwrite it.
