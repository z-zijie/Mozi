interface:
  display_name: "Review Operator SPEC"
  short_description: "Score a Mozi operator SPEC quality report"
  default_prompt: "Use $mozi:review-spec with an absolute SPEC path to score SPEC quality and return YAML only."
